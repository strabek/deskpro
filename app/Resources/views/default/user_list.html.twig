{% extends 'base.html.twig' %}

{% block body %}
  <table>
    {% for user in users %}
      <tr>
        <td>
          <a href="{{ path('user_details', {id: user.id}) }}">{{ user.name }}</a>
          --
          <a href="{{ path('user_update', {id: user.id}) }}">update</a>
          --
          <a href="{{ path('user_delete', {id: user.id}) }}">delete</a>
        </td>
      </tr>
      <tr>
      {% endfor %}
    </table>
    {% block stylesheets %}{% endblock %}

  {% endblock %}
